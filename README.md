# 🍽️ HomeMade Food Delivery

Telegram бот и веб-приложение для заказа домашней еды в Abu Dhabi.

## 📋 Описание

HomeMade - это полнофункциональная платформа для заказа домашней еды, включающая:
- 🤖 **Telegram бот** для администрирования заказов
- 🌐 **Web приложение** (FastAPI) с красивым интерфейсом
- 📱 **Telegram Mini App** для пользователей
- 💾 **SQLite база данных** для хранения заказов и продуктов

## 🚀 Быстрый старт

### Требования

- Python 3.9+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <your-repo-url>
cd HomeMade
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**

Создайте файл `.env` в корне проекта:
```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321

# Database
DATABASE=homefood.db

# API
API_HOST=0.0.0.0
API_PORT=8000

# Web App URL
WEB_APP_URL=https://your-domain.com
```

5. **Запустите приложение**

Есть два варианта:

**Вариант 1: Используя новые файлы (рекомендуется)**
```bash
# Терминал 1 - Запуск Web API
python main_new.py

# Терминал 2 - Запуск Telegram бота
python bot_new.py
```

**Вариант 2: Используя старые файлы**
```bash
# Терминал 1 - Запуск Web API
python main.py

# Терминал 2 - Запуск Telegram бота
python bot.py
```

## 📁 Структура проекта

```
HomeMade/
├── src/                          # Исходный код
│   ├── config/                   # Конфигурация
│   │   ├── __init__.py
│   │   └── settings.py          # Настройки из .env
│   │
│   ├── database/                # База данных
│   │   ├── __init__.py
│   │   ├── db.py               # Работа с БД
│   │   └── models.py           # Модели данных
│   │
│   └── bot/                     # Telegram бот
│       └── utils/               # Утилиты
│           ├── __init__.py
│           ├── helpers.py      # Вспомогательные функции
│           └── notifications.py # Отправка уведомлений
│
├── static/                      # Статические файлы
│   └── stickers_animations/    # Анимации Lottie
│
├── bot.py                       # Старый бот (legacy)
├── bot_new.py                   # Новый бот (рекомендуется)
├── main.py                      # Старое API (legacy)
├── main_new.py                  # Новое API с уведомлениями
├── requirements.txt             # Зависимости Python
├── .env                         # Переменные окружения (создать)
├── .gitignore
└── README.md                    # Этот файл
```

## 🔑 Основные возможности

### Для пользователей

- ✅ Просмотр меню по категориям (бургеры, пицца, плов, супы и т.д.)
- ✅ Добавление товаров в корзину
- ✅ Оформление заказа с указанием адреса доставки
- ✅ **Автоматическое уведомление о статусе заказа в Telegram**
- ✅ Просмотр своих заказов через бота (кнопка "Мои заказы")
- ✅ Связь с поварами через WhatsApp

### Для администраторов

- ✅ Просмотр всех заказов
- ✅ Фильтрация заказов по статусу (новые, в работе, готовые)
- ✅ Изменение статуса заказов через кнопки
- ✅ **Автоматические уведомления о новых заказах**
- ✅ Статистика по заказам
- ✅ Управление меню (добавление/удаление блюд)

## 🔄 Как работают уведомления

### При создании заказа через мини-апп:

1. Пользователь оформляет заказ
2. Создается запись в БД с уникальным ID
3. **Админы получают уведомление** с полной информацией о заказе
4. **Пользователь получает уведомление** со статусом и номером заказа
5. Повара получают уведомление в WhatsApp

### При изменении статуса:

- Админ нажимает кнопку изменения статуса
- Статус обновляется в БД
- Отображается обновленная информация

## 🗄️ База данных

### Таблицы:

**orders** - заказы
- `id` - уникальный ID заказа
- `customer_name` - имя клиента
- `customer_telegram` - Telegram username
- `customer_phone` - телефон
- `customer_address` - адрес доставки
- `user_telegram_id` - Telegram ID пользователя (для уведомлений)
- `total_amount` - сумма заказа
- `status` - статус (pending, confirmed, cooking, ready, delivered, cancelled)
- `created_at` - дата создания

**order_items** - позиции заказа
- `order_id` - ID заказа
- `product_id` - ID продукта
- `product_name` - название продукта
- `quantity` - количество
- `price` - цена за единицу
- `cook_telegram` - Telegram повара
- `cook_name` - имя повара
- `cook_phone` - телефон повара

**products** - продукты
- `id` - ID продукта
- `name` - название
- `description` - описание
- `price` - цена
- `image` - URL изображения
- `cook_telegram` - Telegram повара
- `category` - категория (burger, pizza, plov, etc.)
- `ingredients` - ингредиенты (JSON)

## 🛠️ API Endpoints

### Products
- `GET /api/products` - получить все продукты
- `GET /api/products?category=burger` - продукты по категории
- `GET /api/products/{id}` - конкретный продукт

### Orders
- `POST /api/orders` - создать заказ (с уведомлениями!)
- `GET /api/orders` - все заказы
- `GET /api/orders/{id}` - конкретный заказ
- `PUT /api/orders/{id}/status` - изменить статус

### Web App
- `GET /` - главная страница
- `GET /app` - мини-апп (категории)
- `GET /app/{category}` - страница категории

## 📱 Telegram Bot команды

### Для всех пользователей:
- `/start` - запуск бота и главное меню

### Для администраторов:
- `/orders` - все заказы
- `/pending` - новые заказы
- `/stats` - статистика
- `/products` - управление меню
- `/addproduct` - добавить блюдо

## 🎨 Категории блюд

- 🍔 **burger** - Бургеры
- 🍕 **pizza** - Пицца
- 🍚 **plov** - Плов
- 🍲 **soup** - Супы
- 🥟 **pelmeni** - Пельмени
- 🥖 **khachapuri** - Хачапури
- 🍰 **dessert** - Десерты
- 🥗 **salad** - Салаты

## 🔐 Получение Telegram ID

Для настройки админов нужен Telegram ID:

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Скопируйте ваш ID
4. Добавьте в `.env` файл в `ADMIN_IDS`

## 🚀 Деплой

### Railway / Render / Heroku

1. Создайте `Procfile`:
```
web: python main_new.py
worker: python bot_new.py
```

2. Установите переменные окружения на платформе

3. Деплойте!

## 🐛 Решение проблем

### Бот не запускается
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь, что токен валидный

### Уведомления не приходят
- Проверьте `ADMIN_IDS` в `.env`
- Убедитесь, что пользователь написал боту `/start`

### База данных
- При первом запуске БД создается автоматически
- Для сброса БД удалите файл `homefood.db`

## 📝 TODO

- [ ] Добавить payment integration
- [ ] Добавить фото заказов
- [ ] История изменения статусов
- [ ] Email уведомления
- [ ] Панель аналитики

## 👨‍💻 Разработка

Автор: HomeMade Team
Telegram: [@sekeww](https://t.me/sekeww)

## 📄 Лицензия

MIT License
